{% extends "themes/base_admin.html.twig" %}

{% block title %}Le blog de Jean Dupont - Admin - Tableau de bord{% endblock %}

{% block description %}Le blog de Jean Dupont - Admin - Tableau de bord{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Hello {{app.user.firstName}}</h1>

    <div class="container my-5">
        <div class="row">
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none" href="{{path('admin.category.index')}}">
                        <div class="card-header">Les catégories</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{categories|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none" href="{{path('admin.post.index')}}">
                        <div class="card-header">Les articles</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{posts|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none" href="{{path('admin.tag.index')}}">
                        <div class="card-header">Les tags</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{tags|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none" href="{{path('admin.comment.index')}}">
                        <div class="card-header">Les commentaires</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{comments|length}}</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none" href="{{path('admin.user.index')}}">
                        <div class="card-header">Les utilisateurs</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{users|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none" href="{{path('admin.contact.index')}}">
                        <div class="card-header">Les contacts</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{contacts|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <div class="card-header">Les likes</div>
                    <div class="card-body">
                        <p class="card-text">{{postLikes|length}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}